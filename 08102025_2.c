#include <stdio.h>
int main()
{
int r,c,h,th;
printf("Enter height of triangle");
scanf("%d", &h);
th = (h*2) - 1 ;
for (r = 1; r <= h; r++) {
	c = 1;
	while (c>=r/2 & c <= ){
		printf("#");
		c++;
		}
	}
	printf("\n");
    r++;
}
return 0;
}